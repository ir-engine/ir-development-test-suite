{
  "name": "@ir-engine/ir-development-test-suite",
  "version": "0.0.0",
  "type": "module",
  "description": "Assets and tests for Infinite Reality Engine core development",
  "main": "",
  "scripts": {
    "check-errors": "tsc --noemit",
    "test": "vitest run",
    "format": "prettier --write \"**/*.{ts,tsx}\"",
    "format-scss": "stylelint \"**/*.scss\" --fix",
    "format-staged": "lint-staged"
  },
  "pre-commit": [
    "format-staged"
  ],
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write \"**/*.{ts,tsx}\""
    ],
    "*.scss": [
      "stylelint \"**/*.scss\" --fix"
    ]
  },
  "license": "ISC"
}
